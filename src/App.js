import React, { useRef } from "react";

// component imports
import Hero from "./components/Hero";

const App = () => {
  return (
    <div>
      <Hero appContainer />
    </div>
  );
};

export default App;
